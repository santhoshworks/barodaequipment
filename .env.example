# Base URL for the auth/backend API.
# Leave empty to use the built-in mock auth (OTP "123456" or any 6 digits works).
# Set this when wiring the real backend, e.g. https://api.yaazhtech.com
NEXT_PUBLIC_API_BASE_URL=
